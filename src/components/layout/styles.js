import makeStyles from "@mui/styles/makeStyles";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "100vw",
    // height: "100vh",
    // display: "flex",
  },
 
  mainContent: {
    // padding: "8px",
    width: "100%",
    height: "100%",
    marginTop:"56px",
    padding:"10px",
  },
}));

export { useStyles };
